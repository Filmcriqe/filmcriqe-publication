import { Entry } from '@/lib/content'; import { Card } from '@/components/content';
export function Archive({title,intro,items,filters=false}:{title:string;intro:string;items:Entry[];filters?:boolean}){return <main className="shell"><header className="page-head"><div className="eyebrow">Filmcriqe archive</div><h1 className="display">{title}</h1><p className="lede">{intro}</p></header>{filters&&<form className="filters"><input aria-label="Search reviews" placeholder="Search reviews"/><select aria-label="Filter by genre"><option>All genres</option><option>Drama</option><option>Documentary</option></select><select aria-label="Sort reviews"><option>Newest first</option><option>Oldest first</option><option>Highest SceneScore</option></select></form>}<div className="archive-grid">{items.map(x=><Card entry={x} key={x.slug}/>)}</div></main>}
