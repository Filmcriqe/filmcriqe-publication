import Link from 'next/link'; import { Entry, labels } from '@/lib/content'; import { EditorialImageSlot } from '@/components/editorial-image';
export const ImageSlot=EditorialImageSlot;
export function Card({entry,variant=''}:{entry:Entry;variant?:string}){const href=entry.type==='review'?`/reviews/${entry.slug}`:entry.type==='hidden-gem'?`/hidden-gems/${entry.slug}`:`/${entry.type}s/${entry.slug}`;return <article className={`card ${variant}`}><ImageSlot slot={`content.${entry.slug}`} src={entry.image} alt={entry.imageAlt} focalPoint={entry.focalPoint}/><div className="eyebrow">{labels[entry.type]}</div><h2><Link href={href}>{entry.title}</Link></h2><p>{entry.dek}</p><div className="metadata">{entry.author?`By ${entry.author} · `:''}{entry.year || entry.date}{entry.score?` · SceneScore ${entry.score}`:''}</div></article>}
export function StarRating({value=4.5}:{value?:number}){const full=Math.round(value); return <span className="stars" aria-label={`${value} out of 5 stars`}>{'★'.repeat(full)}{'☆'.repeat(5-full)}</span>}
