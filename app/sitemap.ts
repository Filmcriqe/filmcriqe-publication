import type { MetadataRoute } from 'next'; import { entries } from '@/lib/content'; export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||'https://filmcriqe.com';return ['','/reviews','/features','/essays','/interviews','/hidden-gems','/about','/newsletter','/contact'].map(url=>({url:base+url,lastModified:new Date()})).concat(entries.map(e=>({url:`${base}/${e.type==='hidden-gem'?'hidden-gems':e.type+'s'}/${e.slug}`,lastModified:new Date(e.date)})))}
