import Link from 'next/link'; import { entries } from '@/lib/content'; import { Card, ImageSlot } from '@/components/content'; import { NewsletterForm } from '@/components/public-forms';
export default function Home(){const lead=entries[1],review=entries[0],gems=entries.filter(x=>x.type==='hidden-gem');return <main><section className="shell hero lead-story"><div><div className="eyebrow">Lead story · {lead.type}</div><h1 className="display">{lead.title}</h1><p>{lead.dek}</p><div className="metadata">By {lead.author} · {lead.date}</div><Link className="text-link" href={`/features/${lead.slug}`}>Read story →</Link></div><ImageSlot slot="home.lead" src={lead.image} alt={lead.imageAlt} focalPoint={lead.focalPoint} /></section><section className="shell section"><div className="section-title"><div><div className="eyebrow">New dispatches</div><h2 className="display">Latest</h2></div><Link href="/search" className="text-link">Search archive</Link></div><div className="archive-grid editorial-grid">{entries.slice(0,3).map((e,i)=><Card entry={e} variant={i===0?'featured-card':''} key={e.slug}/>)}</div></section><section className="shell editorial"><ImageSlot slot="home.featured-review" src={review.image} alt={review.imageAlt} focalPoint={review.focalPoint}/><div><div className="eyebrow">Featured review</div><h2 className="display">{review.title}</h2><p>{review.dek}</p><div className="metadata">SceneScore {review.score} · {review.year}</div><Link href={`/reviews/${review.slug}`} className="text-link">Read review →</Link></div></section><section className="shell section"><div className="section-title"><div><div className="eyebrow">Worth another look</div><h2 className="display">Hidden Gems</h2></div><Link href="/hidden-gems" className="text-link">Explore all</Link></div><div className="archive-grid">{gems.map(e=><Card entry={e} key={e.slug}/>)}</div></section><section className="shell editorial upcoming"><ImageSlot slot="home.upcoming" src="/editorial/hollywood.jpg" alt="Black-and-white photograph of the Hollywood sign at night."/><div><div className="eyebrow">Coming soon</div><h2 className="display">Films on the horizon.</h2><p>A considered watchlist of films FILMCRIQE plans to review.</p><Link href="/reviews" className="text-link">Browse reviews →</Link></div></section><section className="shell editorial from-filmcriqe"><ImageSlot slot="home.profile" src="/editorial/road.jpg" alt="Black-and-white photograph of cars moving along an open road." className="portrait"/><div><div className="eyebrow">From Filmcriqe</div><h2 className="display">A publication for close looking.</h2><p>Reviews, essays and conversations that make room for cinema’s details.</p><Link href="/about" className="text-link">About Filmcriqe →</Link></div></section><section className="shell manifesto"><p className="display">“Good films don’t just tell stories.<br/>They change the way we see the world.”</p><div className="eyebrow">Filmcriqe manifesto</div></section><section className="shell letter"><div><div className="eyebrow">Review News</div><h2 className="display">New reviews, essays and hidden gems.</h2></div><NewsletterForm compact/></section></main>}
