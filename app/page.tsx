import Link from 'next/link'; import { entries } from '@/lib/content'; import { Card, ImageSlot } from '@/components/content';
export default function Home(){const featured=entries[0], editorial=entries[1];return <main><section className="shell hero"><div><div className="eyebrow">Featured Review</div><h1 className="display">{featured.title}</h1><p>{featured.dek}</p><div className="metadata">{featured.director} · {featured.year}</div><Link className="text-link" href={`/reviews/${featured.slug}`}>Read Review →</Link></div><ImageSlot label="Featured review photography — chosen in Homepage" /></section><section className="shell section"><div className="section-title"><div><div className="eyebrow">New dispatches</div><h2 className="display">Latest</h2></div><Link href="/search" className="text-link">Search archive</Link></div><div className="archive-grid">{entries.slice(0,3).map(e=><Card entry={e} key={e.slug}/>)}</div></section><section className="shell editorial"><ImageSlot label="Feature photography — chosen in Homepage"/><div><div className="eyebrow">Editorial</div><h2 className="display">{editorial.title}</h2><p>{editorial.dek}</p><Link href={`/features/${editorial.slug}`} className="text-link">Read Feature →</Link></div></section><section className="shell manifesto"><p className="display">“Good films don’t just tell stories.<br/>They change the way we see the world.”</p><div className="eyebrow">Filmcriqe manifesto</div></section><section className="shell letter"><div><div className="eyebrow">The Filmcriqe Letter</div><h2 className="display">A considered dispatch, occasionally.</h2></div><form><label className="sr-only" htmlFor="email">Email address</label><input id="email" type="email" placeholder="Your email address" required/><button className="button">Subscribe</button></form><p className="metadata">Newsletter delivery requires a connected provider. No email is sent from this demo.</p></section></main>}
