import { Archive } from '@/components/archive'; import { publishedByType } from '@/lib/publication-data'; export default async function Gems(){return <Archive title="Hidden Gems" intro="Great films aren’t always the most popular." items={await publishedByType('hidden-gem')}/>}
